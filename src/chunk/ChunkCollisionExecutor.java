package chunk;

import engine.physics.ICollider;
/*
public class ChunkCollisionExecutor extends CollisionExecutor {
    public static final CollisionExecutor CHUNK = new ChunkCollisionExecutor();
    @Override
    public boolean isColliding(ICollider col1, ICollider col2) {
        return super.isColliding(col1, col2);
    }
}

 */
